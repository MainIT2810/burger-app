const stateDefault = {

}

export const BurgerReducer = (state = stateDefault, action) => {
    switch (action.type){
        case "": {
            return {...state};
        }
        default: 
        return {...state};
    }
}